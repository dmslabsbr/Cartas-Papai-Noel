# ===========================================
# CONFIGURAÇÕES PARA APLICAÇÃO APENAS
# Sistema Noel - Serviços Externos
# ===========================================
# Copie este arquivo para .env e configure as variáveis conforme seus serviços externos

# ===========================================
# AMBIENTE
# ===========================================
ENVIRONMENT=production

# ===========================================
# BANCO DE DADOS POSTGRESQL (EXTERNO)
# ===========================================
DB_HOST=seu-servidor-postgres.com
DB_PORT=5432
DB_NAME=noel_db
DB_USER=noel_user
DB_PASSWORD=sua_senha_do_postgres

# ===========================================
# SESSÕES E SEGURANÇA
# ===========================================
SESSION_SECRET_KEY=chave_secreta_para_sessoes_muito_longa_e_aleatoria
# Gere uma chave aleatória: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_MAX_AGE=86400
# Tempo de vida da sessão em segundos (86400 = 24 horas)

# ===========================================
# MINIO OBJECT STORAGE (EXTERNO)
# ===========================================
MINIO_ENDPOINT=https://seu-servidor-minio.com:9000
MINIO_ACCESS_KEY=seu_usuario_minio
MINIO_SECRET_KEY=sua_senha_minio
MINIO_BUCKET=cartas
MINIO_SECURE=true

# ===========================================
# LDAP API (EXTERNO)
# ===========================================
LDAP_API_URL=https://seu-servidor-ldap.com:8080

# ===========================================
# CONFIGURAÇÕES DE MINIATURAS
# ===========================================
THUMB_SIZE=150x200
# Tamanho das miniaturas (largura x altura)

# ===========================================
# EXEMPLOS DE CONFIGURAÇÃO
# ===========================================

# Exemplo 1: Serviços locais (desenvolvimento)
# DB_HOST=localhost
# DB_PORT=5432
# MINIO_ENDPOINT=http://localhost:9000
# MINIO_SECURE=false

# Exemplo 2: Serviços na mesma rede Docker
# DB_HOST=postgres-container
# DB_PORT=5432
# MINIO_ENDPOINT=http://minio-container:9000
# MINIO_SECURE=false

# Exemplo 3: Serviços em servidores diferentes
# DB_HOST=db.exemplo.com
# DB_PORT=5432
# MINIO_ENDPOINT=https://storage.exemplo.com:9000
# MINIO_SECURE=true

# ===========================================
# INSTRUÇÕES DE USO
# ===========================================
# 1. Copie este arquivo: cp env.app-only.example .env
# 2. Configure as variáveis com seus servidores externos
# 3. Execute: docker compose -f docker-compose.app-only.yml up -d
# 4. Acesse: http://localhost:8000
